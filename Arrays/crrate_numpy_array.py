# Create a new array of length 5 using numpy arrays and print all elements

# import numpy as np
# Requires: numpy
# Install numpy using pip: pip install numpy

# import numpy
import numpy as np

# Create an array of length 5 with integers
arr = np.array([10, 20, 30, 40, 50])

# Also you can specify the data type of the array elements
# arr = np.array([10, 20, 30, 40, 50], dtype=np.int32)

# Print all elements of the array
print(arr)

# For numerical computations, numpy arrays are usually preferred over Python lists.